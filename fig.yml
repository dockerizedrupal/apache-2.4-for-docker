httpd:
  image: simpledrupalcloud/httpd:latest
  hostname: httpd
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - httpddata
  environment:
    - SERVER_NAME=localhost
httpddata:
  image: simpledrupalcloud/data:latest
  hostname: httpddata
  volumes:
    - /httpd
