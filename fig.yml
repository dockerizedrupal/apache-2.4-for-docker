httpd:
  image: simpledrupalcloud/httpd:latest
  hostname: httpd
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - httpddata
httpddata:
  image: simpledrupalcloud/data:latest
  hostname: httpddata
  volumes:
    - /httpd/data
