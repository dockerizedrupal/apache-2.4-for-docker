apache:
  image: dockerizedrupal/apache-2.4:1.0.9
  hostname: apache
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - apache-data
  environment:
    - SERVER_NAME=localhost
    - TIMEOUT=300
    - PROTOCOLS=https,http
    - INDEXES=Off
    - USER_ID=
    - GROUP_ID=
apache-data:
  image: dockerizedrupal/data:1.0.3
  hostname: apache-data
  volumes:
    - /apache/data
    - /apache/ssl
