httpd:
  image: viljaste/httpd:dev
  hostname: httpd
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - httpddata
  environment:
    - SERVER_NAME=localhost
httpddata:
  image: viljaste/data:dev
  hostname: httpddata
  volumes:
    - /httpd
